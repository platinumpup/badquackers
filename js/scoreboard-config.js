/*
  js/scoreboard-config.js

  NOTE: This file previously contained a real Firebase API key and project details — those are secrets for your project and should not be committed to the repository.

  This replacement file intentionally does NOT contain any secret values. To run the scoreboard locally or in CI, create a new file at js/scoreboard-config.js (which is gitignored) with your Firebase config, or set up a server-side proxy instead.

  Example (do NOT commit):
  window.SCOREBOARD_CONFIG = {
    apiKey: "YOUR_FIREBASE_API_KEY",
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project.appspot.com"
  };
*/

console.warn("scoreboard-config.js has been removed from the repository for security; provide a local js/scoreboard-config.js or use a server-side scoreboard.");
